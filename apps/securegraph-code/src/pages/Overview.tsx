// The app-launcher landing page is identical across Attack, Defend and Code
// (each passes its own `application` key + nav); it lives in @sg so the three
// stay in sync instead of hand-copied.
export { default } from "@sg/pages/AppLauncher";
