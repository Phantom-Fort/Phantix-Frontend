PORT=8787
# Max orgs that can hold a seat (pending + approved)
MAX_SEATS=20
# Shared secret for staff portal (send as X-Sandbox-Staff-Key)
STAFF_API_KEY=change-me-to-a-long-random-string
# Optional CORS origins (comma-separated). Empty = allow all (dev).
# CORS_ORIGINS=https://phantix.site,https://staff.phantix.site,http://localhost:5173
