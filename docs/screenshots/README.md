# Screenshot inventory

Captured with **Google Chrome (headless)** via Playwright against the live Phantix sites, after
authenticating with a real account (email + password + email OTP).

## Root

| File | Surface | Notes |
|------|---------|-------|
| `landing-home.png` | Landing | Home / hero |
| `landing-how-it-works.png` | Landing | How it works section |
| `landing-capabilities.png` | Landing | Capabilities section |
| `landing-pricing.png` | Landing | Pricing section |
| `landing-footer.png` | Landing | Footer |
| `platform-login.png` | Platform | Sign-in form |
| `staff-login.png` | Staff | Staff sign-in form |
| `app-login.png` | Command Centre | Application sign-in form |
| `app-dashboard.png` | Command Centre | Dashboard (may show login/partial — app login blocked by backend issue) |

## Platform (`platform/`)

Authenticated pages: `dashboard`, `identity`, `users`, `connections`, `companies`, `billing`,
`ai-settings`, `audit`, `support`, `tools`, `alerts`.

## Staff (`staff/`)

Authenticated pages: `dashboard`, `clients`, `billing`, `ai-admin`, `vapt-admin`, `compliance`,
`tooling`, `discovery`, `scanner-tools`, `experience`, `server`, `logs`, `super-logs`,
`engine-jobs`, `support`, `staff-users`, `terminal`, `bus-diagnostics`.

## Regenerating

Screenshots can be regenerated by re-running the Playwright capture scripts (see
`docs/USER_MANUALS/README.md` and the capture tooling notes in the project backlog). To capture the
Command Centre, the app-realm login must be fixed (currently failing with "Failed to fetch" on the
OTP verification step).
